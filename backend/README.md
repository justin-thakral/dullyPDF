## Backend

This backend hosts the FastAPI service (`backend/main.py`).

- Field detection pipeline code + docs live in `backend/fieldDetecting/`.
- Firebase auth and database helpers live in `backend/firebaseDB/`.

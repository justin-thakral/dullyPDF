# Sandbox docs

This folder documents how the sandbox PDF pipeline works and how to run it locally.

Start here:
- `backend/fieldDetecting/docs/pipelines.md` for **native vs scanned** and routing behavior.
- `backend/fieldDetecting/docs/running.md` for **commands**, flags, and output artifacts.
- `backend/fieldDetecting/docs/code-map.md` for a **module-by-module map** of the sandbox code.
- `backend/fieldDetecting/docs/rename-flow.md` for the **overlay + rename** OpenAI workflow.
- `backend/fieldDetecting/docs/checkBoxHintArrowBox.md` for the **legacy checkbox callout overlay** and revert steps.
- `backend/fieldDetecting/docs/tools.md` for **maintenance scripts and migration helpers**.
- `backend/fieldDetecting/docs/security.md` for **production hardening guidance**.
- `backend/fieldDetecting/docs/images/` for **debug reference screenshots**.

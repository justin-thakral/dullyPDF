# Code map (what each module does)

This file is a quick guide to the sandbox codebase layout and how the modules fit together.

## Directory layout

- `backend/fieldDetecting/sandbox/combinedSrc/`: Shared preprocessing + utilities used by both pipelines.
- `backend/fieldDetecting/sandbox/nativeSrc/`: Native/text-layer resolver pipeline.
- `backend/fieldDetecting/sandbox/scannedSrc/`: Scanned pipeline.
- `backend/fieldDetecting/sandbox/debug/`: CLI tooling for local runs and QA overlays.
- `backend/fieldDetecting/sandbox/tools/`: Maintenance/batch scripts (sorting, migration, etc.).
- `backend/fieldDetecting/sandbox/test/`: Unit tests for core geometry/coordinate logic.
- `backend/fieldDetecting/forms/`: PDFs generated by sandbox tooling.
- `backend/fieldDetecting/outputArtifacts/`: Default output location (`json/` and `overlays/`).
- `backend/fieldDetecting/docs/images/`: Reference screenshots used when tuning heuristics/prompts.
- `backend/fieldDetecting/docs/tools.md`: Reference for maintenance scripts.

## Core data contracts

### Candidates (detection output)

Candidates are stored per page with stable IDs:
- `line-<page>-<n>`: underline candidates (text inputs)
- `checkbox-<page>-<n>`: checkbox square candidates
- `box-<page>-<n>`: rectangle candidates (signature/date/table cells)

The main assembly happens in `backend/fieldDetecting/sandbox/combinedSrc/build_candidates.py`.

### Fields (resolver output)

Fields are what downstream injectors consume:
- `name`: final field name
- `type`: `text | checkbox | signature | date`
- `page`: 1-based
- `candidateId`: which candidate the field is backed by
- `rect`: computed deterministically from the candidate
- `confidence`: model/heuristic confidence

Rect building rules are centralized in `backend/fieldDetecting/sandbox/combinedSrc/rect_builder.py`.

## Execution flow (end-to-end)

The orchestrator is `backend/fieldDetecting/sandbox/combinedSrc/pipeline_router.py`.

It does:
1) `render_pdf_to_images` (`backend/fieldDetecting/sandbox/combinedSrc/render_pdf.py`)
2) `detect_geometry` (`backend/fieldDetecting/sandbox/combinedSrc/detect_geometry.py`)
3) `extract_labels` (`backend/fieldDetecting/sandbox/combinedSrc/extract_labels.py`)
4) `assemble_candidates` (`backend/fieldDetecting/sandbox/combinedSrc/build_candidates.py`)
5) `compute_label_height_calibration` (`backend/fieldDetecting/sandbox/combinedSrc/calibration.py`)
6) Route to native or scanned pipeline

## Key modules (combinedSrc)

- `backend/fieldDetecting/sandbox/combinedSrc/config.py`
  - Sandbox config + `.env` bootstrap + logging toggle (`SANDBOX_DEBUG`).

- `backend/fieldDetecting/sandbox/combinedSrc/render_pdf.py`
  - Renders PDF pages to images at `SANDBOX_DPI`.

- `backend/fieldDetecting/sandbox/combinedSrc/coords.py`
  - Converts between pixel bboxes and point bboxes and handles rotation/origin conventions.

- `backend/fieldDetecting/sandbox/combinedSrc/detect_geometry.py`
  - OpenCV geometry detection for lines/boxes/checkboxes.
  - Emits bboxes in point space (originTop) and includes pixel bboxes for debug.

- `backend/fieldDetecting/sandbox/combinedSrc/extract_labels.py`
  - Extracts prompt/label text, using OCR fallback (`backend/fieldDetecting/sandbox/combinedSrc/ocr_labels.py`) when needed.

- `backend/fieldDetecting/sandbox/combinedSrc/label_index.py`
  - Spatial index over label midpoints to speed up “nearest label to candidate” lookups.

- `backend/fieldDetecting/sandbox/combinedSrc/heuristic_resolver.py`
  - Deterministic resolver for both pipelines.
  - Focuses on high recall and stable geometry; naming is best-effort.

- `backend/fieldDetecting/sandbox/combinedSrc/openai_utils.py`
  - OpenAI Responses API helpers:
    - temperature fallback (some models reject `temperature`)
    - robust `output_text` extraction

- `backend/fieldDetecting/sandbox/combinedSrc/vision_utils.py`
  - Shared image helpers (encode to data URLs, crop with padding).

- `backend/fieldDetecting/sandbox/combinedSrc/field_overlay.py`
  - Produces an overlay image with candidate boxes and field labels for LLM input.

- `backend/fieldDetecting/sandbox/combinedSrc/rename_resolver.py`
  - Page-level rename resolver:
    - renders one full-page overlay per page
    - calls OpenAI with a strict line-based format
    - returns renamed fields with confidence gating

- `backend/fieldDetecting/sandbox/combinedSrc/output_layout.py`
  - Standardizes output layout under `json/` + `overlays/` and provides temp prefixes.

- `backend/fieldDetecting/sandbox/combinedSrc/form_filler.py`
  - Sandbox-only PDF injection tool that writes actual AcroForm fields into a PDF.
  - Not used by the production pipeline; intended for offline debugging and prototyping.

## Native pipeline (nativeSrc)

- `backend/fieldDetecting/sandbox/nativeSrc/pipeline.py`
  - Entry point for native pipeline; deterministic heuristics only.

## Scanned pipeline (scannedSrc)

- `backend/fieldDetecting/sandbox/scannedSrc/pipeline.py`
  - Entry point for scanned pipeline; deterministic heuristics only.

## Debug tooling (debug)

- `backend/fieldDetecting/sandbox/debug/test_local.py`
  - Primary CLI for local runs; writes `temp<first5><last5>_fields.json`, `temp<first5><last5>_candidates.json`, and can run:
    - `--openAI`

- `backend/fieldDetecting/sandbox/debug/debug_overlay.py`
  - Draws QA overlays (candidates and final fields) for human inspection.

## Maintenance tools (tools)

- `backend/fieldDetecting/sandbox/tools/sort_pdfs.py`
  - Classifies PDFs into `pdfs/native` and `pdfs/scanned` (and optionally form PDFs).

- `backend/fieldDetecting/sandbox/tools/migrate_ml_pdfs.py`
  - Moves ML corpus PDFs into the `pdfs/native|scanned` layout.

- `backend/fieldDetecting/sandbox/tools/migrate_analysis_artifacts.py`
  - Migrates legacy `analysis_*` runs into `outputArtifacts/json` + `outputArtifacts/overlays`.

- `backend/fieldDetecting/sandbox/tools/artifactManager.py`
  - Cleans temp-prefixed artifacts and prunes empty directories.
 

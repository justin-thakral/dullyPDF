# runs/

Generated artifacts from detector runs. This directory is not source-controlled output.

## Cleanup

```bash
python3 runs/cleanOutput.py --detect
```

Use `--all` to clear every run artifact or `--dry-run` to preview.
